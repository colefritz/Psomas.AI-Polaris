name: Python application

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  test_linux:
    runs-on: ubuntu-latest
    steps:
      # Step 1: Check out the repository
      - uses: actions/checkout@v3

      # Step 2: Set up Python 3.11
      - name: Set up Python 3.11
        uses: actions/setup-python@v3
        with:
          python-version: "3.11"

      # Step 3: Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      # Step 4: Sanitize and Load Environment Variables
      - name: Load environment variables from .env
        run: |
          # Write secret to .env file
          echo "${{ secrets.ENV_FILE }}" > .env
          
          # Display .env content for debugging (do not do this in production for sensitive data)
          echo "Contents of the .env file (sanitized for debugging):"
          grep -v 'SECRET\|PASSWORD\|TOKEN' .env || true
          
          # Validate the file format
          if ! grep -qE '^[A-Za-z_][A-Za-z0-9_]*=' .env; then
            echo "Invalid .env format" >&2
            exit 1
          fi

          # Debugging to verify the file before sourcing
          echo "Sourcing the .env file..."
          set -x
          set -a
          source .env || true
          set +a
          set +x

      # Step 5: Debug environment variables (Optional)
      - name: Debug environment variables
        run: |
          echo "Loaded environment variables (sanitized for debugging):"
          env | grep -v 'SECRET\|PASSWORD\|TOKEN' || true

      # Step 6: Run tests with pytest
      - name: Test with pytest
        run: |
          export PYTHONPATH=$(pwd)
          coverage run -m pytest -v --show-capture=stdout
          coverage report -m --include=app.py,backend/*,tests/*
          coverage xml

      # Step 7: Generate Code Coverage Report
      - name: Code Coverage Report
        uses: irongut/CodeCoverageSummary@v1.3.0
        with:
          filename: coverage.xml
          badge: true
          fail_below_min: true
          format: markdown
          hide_branch_rate: false
          hide_complexity: true
          indicators: true
          output: both
          thresholds: '50 80'
